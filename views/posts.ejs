<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="d-flex">
        <%-include("./partials/sidebar.ejs")%>
        <div class="container">
            <div class="d-flex flex-column justify-content-center align-items-center p-4 text-center" style="height: 100vh;">
                <div class="d-flex flex-grow-1">
                    <div>
                        <h1>Postari</h1>
                        <h2>Vrei sa adaugi o postare? Apasa <a href="/adaugare"> aici </a></h2>
                        <ul style="list-style: none;">
                            <% posts.forEach(postare => { %>
                                <li class="m-3">
                                    <h2><%= postare.title %></h2>
                                    <p style="overflow-wrap: break-word;"><%= postare.descriere %></p>
                                    <button class="btn btn-success"><a href="/edit/<%= postare.id%>" style="text-decoration: none; color: white;">Editeaza postare</a></button> <button class="btn btn-danger"><a href="/sterge/<%= postare.id%>" style="text-decoration: none; color: white;">Sterge postare</a></button>
                                </li>
                            <% }); %>
                        </ul>
                    </div>
                </div>
                <%- include("./partials/footer.ejs") %>
            </div>
        </div>
    </div>
</body>
</html>